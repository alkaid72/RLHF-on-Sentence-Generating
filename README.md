
```

The report.pdf is the finalized report

the presentation.pptx is the powerpoint used in the presentation of this project



the assets folder includes the data visualized during the training process and the results of the trained model



the trl folder includes the pre-trained language model used in this project



the data folder includes the data used for training in this project

the Data_prepocessing folder in data contains code for data preprocessing



the test folder includes the code that Test the reward model and  tests the final trained language model 

the train_reward_model.py is the code to train reward model

the terminal_main.py uses reinforcement learning to train language models

the utils.py is the code that transform sample data into input data received by the model

the iTrainingLogger.py is a logger during training





